## 1. Breadcrumbs Component

- [x] 1.1 Create `Breadcrumbs.tsx` in `frontend/src/components/common/` with path-to-spanish mapping and route links.
- [x] 1.2 Integrate `Breadcrumbs` inside `frontend/src/layouts/AppLayout.tsx` above the main `<Outlet />`.

## 2. Sidebar Profile Dropdown Menu

- [x] 2.1 Update `Sidebar.tsx` with Headless UI `Menu` dropdown for the user profile section containing "Ver Perfil", "Configuración", and "Cerrar Sesión".

## 3. ProfilePage Implementation & Route Registration

- [x] 3.1 Create `ProfilePage.tsx` in `frontend/src/pages/app/` formatted with Notion Glassmorphism.
- [x] 3.2 Register `/profile` route in `frontend/src/navigation/App.tsx`.

## 4. Verification

- [x] 4.1 Run frontend build and verify clean compilation without TypeScript or routing errors.
