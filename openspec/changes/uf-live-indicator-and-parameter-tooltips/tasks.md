## 1. API Fetch & Live UF Card

- [x] 1.1 Add mindicador.cl UF fetch call using `@tanstack/react-query` (`useQuery`) in `MonthlyParametersPage.tsx`.
- [x] 1.2 Build live UF Notion Glass indicator card above the monthly parameters form showing live value ($), unit, update date, and "Usar valor oficial" button.
- [x] 1.3 Implement auto-fill logic for `ufClosingValue` state upon successful mindicador query response.

## 2. Parameter Field Tooltips

- [x] 2.1 Create custom Tooltip component or Headless UI Popover/Tooltip helper for parameter field labels.
- [x] 2.2 Add info icons `ⓘ` with Chilean payroll tooltips to Period, UF, UTM, Minimum Wage, AFP Cap, AFC Cap, and SIS Rate field labels.

## 3. Verification

- [x] 3.1 Run frontend build (`pnpm build`) and verify clean compilation.
