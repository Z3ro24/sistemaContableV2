## 1. Modular Components Implementation

- [x] 1.1 Create `SidebarItem.tsx` in `frontend/src/components/layout/` with icon, label, and active route detection.
- [x] 1.2 Create `Sidebar.tsx` in `frontend/src/components/layout/` with brand header, navigation list, user profile card, and logout button.

## 2. Layout & Routing Integration

- [x] 2.1 Create `AppLayout.tsx` in `frontend/src/layouts/` combining `Sidebar` and `<Outlet />`.
- [x] 2.2 Update `frontend/src/navigation/App.tsx` to wrap private routes inside `AppLayout`.

## 3. Verification

- [x] 3.1 Run frontend build and verify clean compilation without TypeScript or routing errors.
